const SearchBox = () => {
  return (
    <div>
      <label htmlFor="find">
        <span>Find contacts by name</span>
        <input type="text" name="find" />
      </label>
    </div>
  );
};

export default SearchBox;
